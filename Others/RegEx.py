
# A place for all RegEx types of questions.

# given a string that contains only a's and b's,
# write a regex expression to return true when a string starts and end with the same letter.

reg_pattern = '^[a-b]$|^([a-b]).*\\1$'
